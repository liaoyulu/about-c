#include <stdio.h>
#include <stdlib.h>
#include "iomanip"
#include <iostream>
using namespace std;
#include "LinkList.h"
/*typedef int ElemType; 
在一链表中，已知每个结点含有三个域：data、next和prior，其中prior域为空，设计一个算法，使每个结点的prior指向它的前驱结点，形成双向循环链表。
要求：
①建立一个结点中含有三个域的单链表；
②在主函数中调用此算法，构成双向循环链表；
③在主函数中利用正向和逆向两种方式输出链表中的数据，验证算法的正确性。*/
